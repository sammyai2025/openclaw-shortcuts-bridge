# Copy to .env and replace the token before launching
BRIDGE_BIND=0.0.0.0
BRIDGE_PORT=8787
BRIDGE_TOKEN=replace-with-a-long-random-token
BRIDGE_AGENT_ID=main
BRIDGE_DEFAULT_MODE=light
BRIDGE_MAX_INPUT_CHARS=4000
